# escape=`
FROM mcr.microsoft.com/windows/servercore:ltsc2022

SHELL ["powershell", "-NoProfile", "-Command", "$ErrorActionPreference='Stop';"]

WORKDIR C:\workspace
COPY . C:\workspace

# Install tools (choco + cmake + ninja + VS build tools if needed)
RUN Set-ExecutionPolicy Bypass -Scope Process -Force; `
    [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; `
    iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1')); `
    choco install cmake ninja -y

# Ensure cl.exe environment; often requires VS Build Tools image/setup in practice.
RUN powershell -ExecutionPolicy Bypass -File scripts/windows/build-all.ps1
